# 📅 カレンダー空き日程ボット 総合ガイド

Googleカレンダーと連動して、Discord上で **予定の自動表示**・**空き日程の検索**・**月間カレンダー画像の出力** を行うボットの、構築から利用・トラブル対処までをまとめた総合ドキュメントです。

- **第I部 セットアップ**（管理者向け）… ゼロから作って常時稼働させる手順
- **第II部 利用法**（利用者向け）… コマンドの使い方
- **第III部 エラー対処** … 困ったときの対処

---
---

# 第I部　セットアップ

## 0. 先に：セットアップウィザード

この第I部の作業を案内するウィザードを同梱しています。使い方は2通りあります。

### 手軽に使う

`setup/wizard.html` をダブルクリックで開くだけです。インストールもコマンドも不要です。
手順の案内と、入力値の**形の検査**、設定値の出力ができます。

### 実際に使えるかまで確かめる

```bash
pip install -r requirements.txt
python3 setup_wizard.py
```

同じ画面が開き、加えて**実際にDiscordやGoogleに接続して確かめます**。

HTMLを直接開いただけでは実接続できません。ブラウザから Discord API を呼ぶことが
許可されていない（CORS）ためで、回避できません。特に、不具合の主因である
**カレンダーの共有漏れ**と **MESSAGE CONTENT INTENT の設定忘れ**は、
実接続しないと検出できません。

ステップと、実接続モードで確かめられることは次のとおりです。

| ステップ | その場で確かめること |
|---|---|
| 1. トークン | トークンが有効か（ボット名を表示） |
| 2. INTENT | MESSAGE CONTENT INTENT がONか |
| 3. チャンネル | チャンネル名・サーバー名・種別。希望すればテスト送信 |
| 4. 鍵JSON | JSONが壊れていないか。`client_email` を表示 |
| 5. カレンダー | IDごとに実接続し、**カレンダー名**を表示 |
| 6. 総合検証 | 全項目を通しで再実行 |
| 7. 出力 | Render に貼る環境変数を生成 |

最後にもう一度全体を確かめるのには理由があります。各ステップの検証は「その時点」の結果にすぎず、
途中でトークンを Reset したり別のカレンダーを共有し直すと前の結果は無効になるからです。

### 入力した値の扱い

ウィザードにはトークンや秘密鍵を入れます。次のように扱っています。

- 値はこのPCの外に出ません。HTMLを直接開いた場合は外部への通信が一切発生せず、
  サーバーを使う場合も同じPC上のプロセスが検証します
- ファイルにもログにも保存しません。サーバーはリクエストごとに受け取って捨てています
- ブラウザにも保存しません。**再読み込みすると入力は消えます**
- （サーバーを使う場合）待ち受けは `127.0.0.1` のみで、他のマシンからは接続できません
- （サーバーを使う場合）起動ごとの鍵を全リクエストで照合し、無関係なページからは叩けません
- （サーバーを使う場合）30分間操作がなければ自動で終了します

出力される設定ファイルは秘密情報を含みます。Render に貼り終えたら削除してください。

出力される設定ファイルは、どちらの使い方でもブラウザ内で組み立てて保存されます。

> ウィザードを使わずに手動で進めても構いません。以降の章がその手順です。
> また、設定後に不具合を調べるときは `python3 setup_checks.py` で検証だけを実行できます。

---

## 1. 全体像

```
Googleカレンダー ──(API)──> ボット(Python) ──> Discordチャンネルに表示
                                  ↑
                          Render（24時間稼働）
                                  ↑
                       UptimeRobot（スリープ防止）
```

- 稼働場所は自分のPCではなくRender（クラウド）上。PCを閉じても動き続ける。

### 使用技術

| 要素 | 使うもの |
|------|----------|
| 言語 | Python 3 |
| Discord操作 | discord.py |
| カレンダー取得 | Google Calendar API（google-api-python-client） |
| 画像生成 | Pillow |
| 稼働環境 | Render（Web Service / Free） |
| スリープ防止 | UptimeRobot |
| コード置き場 | GitHub |

---

## 2. 必要なもの一覧

すべて無料で始められます。

- [ ] Discordアカウントと、ボットを入れるサーバー（自分が管理者のもの）
- [ ] Googleアカウント（表示したいカレンダーを持っているもの）
- [ ] GitHubアカウント
- [ ] Renderアカウント
- [ ] UptimeRobotアカウント
- [ ] PC（初期設定・動作確認用。Mac/Windowsどちらでも可）

このガイドで取得する「鍵」や「ID」は次の4つ。あとで環境変数に入れます。

1. **Discord Bot トークン**
2. **Discord チャンネルID**
3. **Google カレンダーID**
4. **サービスアカウントの鍵JSON**（の中身）

---

## 3. Discord側の準備

### 3-1. ボットを作る
1. [Discord Developer Portal](https://discord.com/developers/applications) を開く
2. 「New Application」→ 名前を付けて作成
3. 左メニュー「Bot」→「Reset Token」でトークンを表示し、**コピーして控える** → `DISCORD_BOT_TOKEN`
4. 同じ画面で **「MESSAGE CONTENT INTENT」をON** にして保存（コマンドで本文を読むのに必須）

### 3-2. ボットをサーバーに招待
1. 左メニュー「OAuth2」→「URL Generator」
2. SCOPES で **`bot`**、BOT PERMISSIONS で **`Send Messages`** にチェック
3. 生成URLをブラウザで開き、自分のサーバーを選んで招待

### 3-3. チャンネルIDを取得
1. Discordアプリ → 設定 →「詳細設定」→「開発者モード」をON
2. 表示したいチャンネルを右クリック →「チャンネルIDをコピー」 → `CHANNEL_ID`（数字のみ）

---

## 4. Googleカレンダー側の準備

ボットは「サービスアカウント」というボット専用のGoogleアカウントでカレンダーを読みます。

### 4-1. プロジェクトとAPIの有効化
1. [Google Cloud Console](https://console.cloud.google.com/) を開く
2. プロジェクトを新規作成
3. 「APIとサービス」→「ライブラリ」→ **Google Calendar API** を有効化

### 4-2. サービスアカウントと鍵の作成
1. 「APIとサービス」→「認証情報」
2. 「認証情報を作成」→「サービスアカウント」を作成
3. 作成したものをクリック →「キー」タブ →「鍵を追加」→「新しい鍵を作成」→ **JSON**
4. ダウンロードしたファイルを `service_account.json` にリネームして保管
   → **中身（`{`〜`}` 全部）** が `SERVICE_ACCOUNT_JSON`

> ⚠️ この鍵ファイルは**絶対にGitHubに上げない**。漏れるとカレンダーにアクセスされます。

### 4-3. カレンダーを共有
1. JSON内の `client_email`（`xxx@xxx.iam.gserviceaccount.com`）をコピー
2. Googleカレンダー → 対象カレンダーの「設定と共有」
3. 「特定のユーザーやグループと共有」にその `client_email` を追加（権限は「予定の表示」でOK）
4. 同じ画面の「カレンダーの統合」にある **カレンダーID** をコピー → `CALENDAR_ID`

> 予定がボットに反映されないときは、**その予定がここで共有したカレンダーに入っているか**をまず確認（共有していないカレンダーの予定は見えません）。

### 4-4. 複数のカレンダーをまとめて見る

仕事用とプライベート用を分けている場合など、複数のカレンダーをまとめて扱えます。

1. **カレンダーごとに 4-3 を行う**（すべてのカレンダーを同じ `client_email` に共有する）
2. 得られたカレンダーIDを **「,」で区切って** `CALENDAR_ID` に入れる

```
CALENDAR_ID=aaa@group.calendar.google.com,bbb@group.calendar.google.com
```

改行区切りでも構いません。IDの前後の空白は自動で取り除かれます。

- 共有はカレンダーごとに必要です。**1つでも共有を忘れるとそのカレンダーは読めず**、コマンドの結果に警告が付きます（※後述のエラー対処参照）
- 空き日判定は「**どのカレンダーか1つでも予定があれば埋まっている**」という扱いです
- 1つだけ指定する従来の書き方はそのまま動きます

---

## 5. ファイル構成

ボットの動作に必要なのは最初の**3ファイル**です（残りはセットアップ支援用で、Render では使いません）。

```
（リポジトリ直下）
├── Calendar.py         … ボット本体
├── requirements.txt    … 必要なライブラリ一覧
├── .gitignore          … アップロード除外設定
├── setup_checks.py     … 設定値の検証（ウィザードと単体実行の両方から使う）
├── setup_wizard.py     … セットアップウィザード（ローカルで起動）
└── setup/
    ├── wizard.html     … ウィザードの画面（単体で開いても動きます）
    └── shots/          … 手順のスクリーンショット（無くても動きます）
```

### requirements.txt
```
discord.py
google-api-python-client
google-auth
Pillow
```
> **`Pillow`** は画像生成（`/c`）に必須。無いと `No module named 'PIL'` で起動失敗します。

### .gitignore
```
service_account.json
```
> `service_account.json` は**GitHubに上げず**、中身をRenderの環境変数に貼ります。
> 画像の日本語フォント同梱は不要です（月・日付は数字、曜日は色で表現）。

---

## 6. 環境変数の一覧

ローカルでもRenderでも、次の4つを設定します。

| 変数名 | 内容 | 取得元 |
|--------|------|--------|
| `DISCORD_BOT_TOKEN` | Discordボットのトークン | 3-1 |
| `CHANNEL_ID` | 表示先チャンネルのID（数字） | 3-3 |
| `CALENDAR_ID` | 表示するカレンダーのID（「,」区切りで複数可） | 4-3 / 4-4 |
| `SERVICE_ACCOUNT_JSON` | 鍵JSONの中身を丸ごと（`{`〜`}`） | 4-2 |

> `SERVICE_ACCOUNT_JSON` は最頻出のつまずき箇所。**名前のスペル**と、**値が途中で切れていないか**（末尾が `}` で終わるか）を必ず確認。

---

## 7. ローカルでの動作確認

`Calendar.py` と `service_account.json` を同じフォルダに置いて、ターミナルで次を1行として実行。

```bash
DISCORD_BOT_TOKEN="あなたのトークン" \
CHANNEL_ID="あなたのチャンネルID" \
CALENDAR_ID="あなたのカレンダーID" \
SERVICE_ACCOUNT_JSON="$(cat service_account.json)" \
SSL_CERT_FILE=$(python3 -m certifi) \
python3 Calendar.py
```

必要ライブラリが未インストールなら先に：
```bash
pip install discord.py google-api-python-client google-auth Pillow
```

ログに `ログインしました: ...` が出れば成功。

---

## 8. GitHubへのアップロード

コマンド不要、ブラウザだけで可能です。

1. [github.com](https://github.com) →「New repository」→ 名前を付け **Private** で作成
2. 「Add file」→「Upload files」
3. **3ファイル（Calendar.py / requirements.txt / .gitignore）** をドラッグ＆ドロップ
4. 「Commit changes」

> アップロード後、一覧に **`service_account.json` が無いこと**を必ず確認。あれば削除。

コードを直すときは、GitHub上でファイルを開き鉛筆アイコン（Edit）で編集 → Commit。Renderが自動で再デプロイします。

---

## 9. Renderへのデプロイ

Webページを持たないボットだが、無料枠で常時稼働させるため **Web Service + ダミーWebサーバー同居**（コードに実装済み）。

1. [render.com](https://render.com) → GitHubで登録
2. 「New +」→「Web Service」→ 該当リポジトリを接続
3. 設定：
   - **Build Command**：`pip install -r requirements.txt`
   - **Start Command**：`python Calendar.py`
   - **Instance Type**：Free
   - **Region**：任意（トラブル時はここを変える）
4. 「Environment Variables」で **6章の4つ**を追加
5. 「Create Web Service」で起動

「Logs」で `ログインしました: ...` が出れば成功。発行される `https://〇〇〇.onrender.com` を控える。

> Renderの **Background Worker** は無料枠がないため、Web Serviceを使う。

---

## 10. UptimeRobotで常時稼働

Renderの無料Web Serviceは15分アクセスがないとスリープする。UptimeRobotで起こし続ける。

1. [uptimerobot.com](https://uptimerobot.com) で登録 →「Add New Monitor」
2. 設定：
   - **Monitor Type**：HTTP(s)
   - **URL**：Renderの `https://〇〇〇.onrender.com`
   - **Monitoring Interval**：5 minutes
3. 「Create Monitor」

> サービスを作り直してURLが変わったら、**UptimeRobotのURLも更新**すること。

⚠️ **無料枠＋UptimeRobotは完全ではない**。稼働時間上限やIP事情でまれに停止することがある。確実な常時稼働ならRender有料（月7ドル程度）やKoyeb等の検討を。

---

## 11. カスタマイズできる部分

`Calendar.py` 上部の設定値を変えるだけで挙動を調整できます。

### 表示日数
```python
DAYS_TO_SHOW = 90        # 何日先まで自動表示するか
DAYS_PER_MESSAGE = 25    # 1メッセージあたりの日数（Embed上限のため最大25）
```

### 自動更新の間隔
```python
@tasks.loop(minutes=60)  # 何分ごとに更新チェックするか
```
- 短いほど反映が速いが、**短すぎるとレート制限（429/1015）の原因**。15〜60分が無難。

### 昼・夜の時間帯（コマンド・画像 共通）
```python
DAY_START, DAY_END = 10, 18       # 昼：10:00-18:00
NIGHT_START, NIGHT_END = 18, 24   # 夜：18:00-24:00
```

### 画像の色・寸法
```python
IMG_BG = (250, 245, 230)          # 背景クリーム
IMG_CELL_BG = (255, 253, 246)     # 日付マスの中
IMG_EVENT_COLOR = (120, 180, 235) # 予定の塗り（水色）
IMG_SUN = (210, 70, 55)           # 日曜=赤
IMG_SAT = (70, 112, 205)          # 土曜=青
```
- `render_month_image` 内の `CELL`（マスの一辺）や `LINE`（線の太さ）で寸法も調整可。

### タイムゾーン
```python
JST = timezone(timedelta(hours=9))   # 日本時間(UTC+9)
```

### コマンドの記号・書式
`on_message` 内の正規表現：
```python
re.fullmatch(r"/([nau]?)([\d\-\.:]+)(r|d)?", text)
```
- 先頭 `n/a/u`（時間帯モード）、末尾 `r`（反転）または `d`（伝助形式）を判定。

---
---

# 第II部　利用法

> 💡 **コマンドを忘れたら `/cmds`** と打てば、いつでもコマンド一覧が表示されます。

## 12. 予定の自動表示

チャンネルには、**今日から約3ヶ月（90日）先まで**の予定が自動で表示されます。

- 予定が入っている日はその内容が、入っていない日は「—」が表示されます。
- 表示は**毎日自動で更新**され、常に「今日から3ヶ月先」を表示し続けます。
- こちらで操作は不要です。見るだけでOKです。

---

## 13. 空き日程を調べるコマンド

チャンネルにコマンドを打つと、その場で結果が返ってきます。

### 時間帯の定義

| 区分 | 時間帯 |
|------|--------|
| 昼 | 10:00 – 18:00 |
| 夜 | 18:00 – 24:00 |

「その時間帯に予定が1つでも重なっていれば、埋まっている」と判定します。

### 基本のコマンド

先頭の文字で、調べる時間帯が変わります。

| コマンド | 意味 |
|----------|------|
| `/2026-9` | **昼**が空いている日 |
| `/n2026-9` | **夜**が空いている日 |
| `/a2026-9` | **昼夜どちらも**空いている日（一日空いている日） |
| `/u2026-9` | **昼か夜のどちらか**が空いている日 |

> 月は1桁でもOKです（`/2026-9` でも `/2026-09` でも同じ）。

### 期間の指定

単月のほか、複数月や日単位の範囲でも調べられます。年をまたいでも指定できます。

| 書き方 | 意味 |
|--------|------|
| `/2026-9` | 2026年9月（1ヶ月） |
| `/2026-8:2026-9` | 2026年8月〜9月（月単位の範囲） |
| `/2026-8.15:2026-9.30` | 2026年8月15日〜9月30日（日単位の範囲） |
| `/2026-12:2027-1` | 年をまたいだ範囲 |

**区切りのルール**
- 範囲の区切りは **`:`（コロン）**
- 日にちを指定するときは **`.`（ピリオド）** で月とつなぐ（例：`2026-9.15`）

### 反転（予定がある日を調べる）

末尾に **`r`** を付けると、「空いている日」ではなく「**予定がある日**」を出します。
※ 反転が使えるのは **昼（無印）と夜（`n`）だけ**です。`a`・`u` に付けても無視されます。

| コマンド | 意味 |
|----------|------|
| `/2026-9r` | 昼に予定がある日 |
| `/n2026-9r` | 夜に予定がある日 |
| `/n2026-9:2026-10r` | 9〜10月で、夜に予定がある日 |

### 伝助形式で出す（日程調整用）

末尾に **`d`**（伝助のd）を付けると、空いている日を **1日1行・曜日つき** で出力します。
そのまま日程調整ツール（伝助など）に貼り付けられる形式です。

```
9/9(水)
9/10(木)
9/11(金)
```

- **全モードで使えます**：`/2026-9d`（昼）、`/n2026-9d`（夜）、`/a2026-9d`（昼夜とも）、`/u2026-9d`（昼か夜）。
- 範囲指定と組み合わせられます（例：`/n2026-9:2026-10d`）。
- `r`（反転）と `d` は**同時には使えません**。

---

## 14. 月間カレンダー画像を出す

**`/c`** に続けて年月を打つと、その月の**カレンダー画像**が返ります。

| コマンド | 意味 |
|----------|------|
| `/c2026-9` | 2026年9月のカレンダー画像 |

各日のマスは上下2分割で、**昼が埋まっていれば上半分、夜が埋まっていれば下半分、両方なら全体**が水色に塗られます。日曜が赤、土曜が青です。

---

## 15. コマンド早見表

```
/cmds                コマンド一覧を表示

/2026-9              昼が空いている日
/n2026-9             夜が空いている日
/a2026-9             昼夜とも空いている日
/u2026-9             昼か夜が空いている日

/2026-9r             昼に予定がある日
/n2026-9r            夜に予定がある日

/2026-9d             昼が空いている日（伝助形式・1日1行）
/n2026-9d            夜が空いている日（伝助形式）
/a2026-9d            昼夜とも空いている日（伝助形式）
/u2026-9d            昼か夜が空いている日（伝助形式）

/c2026-9             月間カレンダー画像

/2026-8:2026-9       月単位の範囲
/2026-8.15:2026-9.30 日単位の範囲
/2026-12:2027-1      年をまたぐ範囲
```

先頭（`n`/`a`/`u`/無し）・期間・末尾（`r` または `d`）は組み合わせて使えます。
例：`/n2026-8.15:2026-9.30r` = 8/15〜9/30で夜に予定がある日
（ただし `r` と `d` は併用不可）

---

## 16. 出力の見かた

通常の結果は、月ごとに改行して表示されます。日付は各月の最初だけ「月/日」、以降は日にちだけになります。

```
2026/8/15〜2026/9/30
夜が空いている日（08/25現在）
8/15, 16, 20, 25
9/1, 2, 5, 12, 30
```

末尾の「〇〇現在」は、コマンドを送った時点の日付です。
伝助形式（末尾 `d`）のときは、装飾なしで日付だけが1行ずつ並びます。

---
---

# 第III部　エラー対処

## 17. よくあるトラブルと対処

### `ModuleNotFoundError: No module named 'PIL'`
`requirements.txt` に **`Pillow`** が無い。追加してCommit → 再デプロイ。

### `KeyError: 'SERVICE_ACCOUNT_JSON'`（など）
環境変数の設定漏れか名前のスペルミス。Renderの「Environment」で4つ揃っているか確認。

### `json.decoder.JSONDecodeError`
`SERVICE_ACCOUNT_JSON` の値が途中で切れている。`{`〜`}` を全選択でコピーし直す。

### `Improper token has been passed.`
`DISCORD_BOT_TOKEN` が間違い、または前後に空白・改行が混入。貼り直す。

### `FileNotFoundError: 'service_account.json'`（ローカル時）
鍵ファイルが実行フォルダに無い、または名前違い。同じフォルダに置く。

### `CERTIFICATE_VERIFY_FAILED`（ローカル時）
SSL証明書の問題。`pip install --upgrade certifi` 後、起動時に `SSL_CERT_FILE=$(python3 -m certifi)` を付ける（Renderでは不要）。

### `No open ports detected`（Render）
Web Serviceなのにポートを開いていない。→ ダミーWebサーバー部分が正しく入っているか確認（本体には実装済み）。

### `event registered must be a coroutine function`
`@client.event` の直後の関数が `async def` でない、または間に別コードが割り込んでいる。

### UptimeRobotで `501 Not Implemented`
HEADリクエスト未対応。→ `do_HEAD` がコードにあるか確認（本体には実装済み）。

### `429 Too Many Requests`（Discord）
短時間のアクセス過多。数分〜十数分待つ。更新間隔を延ばし、編集ループの `asyncio.sleep(1)` を維持。

### Cloudflare `1015 / rate limited`（Discord）
IP単位の一時ブロック。**ボットを止めて放置**（数十分〜数時間）。Renderの共有IPが原因のことがあり、その場合は**別リージョンで作り直す**と新しいIPになり解決することが多い。作り直したらUptimeRobotのURLも更新。

### 結果の末尾に「⚠️ 〇件のカレンダーを読み取れませんでした」と出る
`CALENDAR_ID` に指定したカレンダーのうち、読めないものがある。結果は不完全なので、**そのまま日程調整に使わないこと**。
サーバーのログに `カレンダー読み取りエラー: <カレンダーID> -> ...` と出ているので、そのIDについて確認する。

- そのカレンダーを `client_email` に共有したか（**共有漏れが最頑強**。4-4 参照）
- カレンダーIDの綼り・余分な文字はないか
- 区切りの「,」を入れ忘れてIDがつながっていないか

ログが `予定が多すぎて取りきれません` だった場合は別の原因です。そのカレンダーに指定期間内の予定が約5万件以上あるとこの扱いになります。指定する期間を短くしてください。

### 自動表示が更新されない（ログに「更新を見送りました」と出る）
読めないカレンダーが1つでもある間、自動表示は**意図的に更新を見送ります**。
予定が抜けた不完全な表示で上書きするより、前回の正しい表示を残す方が安全という判断です。
上の項目でカレンダーを読めるように直せば、次の更新周期で自動的に復帰します。

### `ValueError: CALENDAR_ID にカレンダーIDが1つも入っていません`
`CALENDAR_ID` が空か、区切り文字と空白だけになっている。少なくとも1つのIDを入れる。

### 予定を変更・追加したのに反映されない
- コマンド／画像は実行時点の最新を参照するが、**その予定が `CALENDAR_ID` に列挙したカレンダーのいずれかに入っているか**をまず確認（列挙していないカレンダーの予定は見えない）。複数指定しているなら、そのカレンダーが `CALENDAR_ID` に入っているかと、`client_email` に共有されているかの両方を見る。
- 自動表示は更新間隔（最大 `minutes` 分）待つ。変更した予定が **90日より先** なら自動表示の範囲外（正常）。
- 予定の時間帯が昼(10-18)・夜(18-24)の境界とどう重なるかで、画像の塗られ方が決まる。

### コマンドに反応しない
- 書式（特に区切りの `:` と `.`）が正しいか確認。
- `r` と `d` を同時に付けていないか（併用不可）。
- Discord Developer Portalで **MESSAGE CONTENT INTENT** がONか確認。

### 結果が長すぎて途切れる
指定する期間を短くする。

---

## 付録：起動の流れ（内部動作）

1. 起動時、環境変数を読み込み、Google/Discordに接続。
2. ダミーWebサーバーを別スレッドで起動（Renderのポート検査＆UptimeRobot対応）。
3. `on_ready` で定期更新タスクを開始。
4. 一定間隔で「今日の日付＋予定内容」を前回と比較し、変化があれば90日分のEmbed（複数メッセージ）を送信または編集。
5. 利用者がコマンドを送ると `on_message` が反応：
   - `/c年-月` → 月間カレンダー画像を生成して返信
   - `/[n/a/u]年-月[:範囲][r|d]` → 空き日（または予定がある日）を計算し、通常形式または伝助形式で返信
