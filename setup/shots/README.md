# スクリーンショット

セットアップウィザード（リポジトリ直下の `wizard.html`）の各ステップに表示される画像です。

`wizard.html` は `shot("ファイル名", "説明")` という形でファイル名を直接参照しています。
差し替えるときはファイル名を変えずに置き換えてください。
画像が無い場合は枠に「画像未設定」と表示され、テキスト手順だけで進める作りなので、
削除しても壊れません。

画面を撮り直すときは、トークン・鍵・メールアドレス・各種IDが写り込んでいないか
確認してから保存してください。
